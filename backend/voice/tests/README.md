# Проверки адаптеров

Offline: injectable provider client; валидация входа, ошибки/пустой результат,
mixed-сохранение текста, выбор API-параметров, корректные MP3 bytes и usage.
Автоматические retries у STT/TTS/Router отключены ради контроля расходов.
Live: `backend/voice/evals/README.md`; явный запуск с лимитом. MP3-файлы
нужно прослушать человеку на demo-машине, отдельно проверить микрофон и шум.
Расход записывать отдельно от текстового routing eval.
Добавленные pytest-тесты этой папки уже входят в общий testpaths.
